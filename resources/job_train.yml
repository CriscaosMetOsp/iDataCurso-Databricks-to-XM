# Clase 10 — entrenamiento reproducible y registro en UC
resources:
  jobs:
    train_demanda:
      name: "[xm-demanda] train_model"
      tasks:
        - task_key: train
          notebook_task:
            notebook_path: ../notebooks/04_train.py
            base_parameters:
              catalog: ${var.catalog}
              horizon_days: ${var.horizon_days}
