# Clase 7 / 12 — Gold features
resources:
  jobs:
    features_demanda:
      name: "[xm-demanda] build_features"
      tasks:
        - task_key: gold_features
          notebook_task:
            notebook_path: ../notebooks/03_gold_features.py
            base_parameters:
              catalog: ${var.catalog}
