# Clase 5 — Lakeflow Declarative Pipeline Bronze -> Silver con expectations
resources:
  pipelines:
    silver_energia:
      name: "[xm-demanda] silver_energia"
      catalog: ${var.catalog}
      target: silver_energia
      serverless: true
      libraries:
        - notebook:
            path: ../notebooks/02_silver_pipeline.py
      configuration:
        catalog: ${var.catalog}
